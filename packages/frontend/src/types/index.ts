export interface Book {
    id: number;
    title: string;
    author: string;
    isbn: string;
}

export interface User {
    id: number;
    username: string;
    email: string;
    fullName: string;
} 